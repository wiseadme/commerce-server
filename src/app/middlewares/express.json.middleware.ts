import express from 'express'
import { IMiddleware } from '@/types/middlewares'

// export class JSONMiddleware implements IMiddleware{
//   execute: IMiddleware['execute'] = express.json.bind(this)
// }
